---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: "{{ include "headlamp.fullname" . }}-admin"
  namespace: {{ .Release.Namespace }}

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: "{{ include "headlamp.fullname" . }}-admin"
subjects:
- kind: ServiceAccount
  name: "{{ include "headlamp.fullname" . }}-admin"
  namespace: {{ .Release.Namespace }}
roleRef:
  kind: ClusterRole
  name: cluster-admin
  apiGroup: rbac.authorization.k8s.io

---
apiVersion: v1
kind: Secret
metadata:
  name: "{{ include "headlamp.fullname" . }}-admin"
  namespace: {{ .Release.Namespace }}
  annotations:
    kubernetes.io/service-account.name: "{{ include "headlamp.fullname" . }}-admin"
type: kubernetes.io/service-account-token

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{ include "headlamp.fullname" . }}"
  namespace: {{ .Release.Namespace }}
  labels:
    app: "{{ include "headlamp.fullname" . }}"
    component: deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "{{ include "headlamp.fullname" . }}"
      component: deployment
  template:
    metadata:
      labels:
        app: "{{ include "headlamp.fullname" . }}"
        component: deployment
    spec:
      serviceAccountName: "{{ include "headlamp.fullname" . }}-admin"
      nodeSelector:
        kubernetes.io/os: linux
      containers:
      - name: headlamp
        image: "{{ .Values.headlamp.image }}:{{ .Values.headlamp.version }}"
        args:
        - -in-cluster
        - -plugins-dir=/headlamp/plugins
        ports:
        - name: http
          containerPort: 4466
        - name: metrics
          containerPort: 9090
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop: [ALL]
        resources:
          limits:
            memory: 128Mi
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 5
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: "{{ include "headlamp.fullname" . }}"
  namespace: {{ .Release.Namespace }}
  labels:
    app: "{{ include "headlamp.fullname" . }}"
    component: service
spec:
  selector:
    app: "{{ include "headlamp.fullname" . }}"
    component: deployment
  ports:
  - name: http
    port: 80
    targetPort: http
  type: ClusterIP

---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: "{{ include "headlamp.fullname" . }}"
  namespace: {{ .Release.Namespace }}
  labels:
    app: "{{ include "headlamp.fullname" . }}"
    component: ingress
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`{{ .Values.headlamp.hostname }}`)
      services:
        - kind: Service
          name: "{{ include "headlamp.fullname" . }}"
          port: 80
