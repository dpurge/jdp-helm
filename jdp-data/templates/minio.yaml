---
kind: Service
apiVersion: v1
metadata:
  name: "{{ include "minio.fullname" . }}"
  namespace: {{ .Release.Namespace }}
spec:
  clusterIP: None
  ports:
  - port: {{ .Values.minio.port }}

---
kind: Endpoints
apiVersion: v1
metadata:
  name: "{{ include "minio.fullname" . }}"
  namespace: {{ .Release.Namespace }}
subsets:
  - addresses:
        - ip: {{ .Values.minio.ip }}
    ports:
      - port: {{ .Values.minio.port }}
        name: "{{ include "minio.fullname" . }}"
