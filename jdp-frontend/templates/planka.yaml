---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ include "planka.fullname" . }}-user-avatars"
  namespace: {{ .Release.Namespace }}
  labels:
    app: "{{ include "planka.fullname" . }}"
    component: pvc
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50M

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ include "planka.fullname" . }}-favicons"
  namespace: {{ .Release.Namespace }}
  labels:
    app: "{{ include "planka.fullname" . }}"
    component: pvc
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20M

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ include "planka.fullname" . }}-background-images"
  namespace: {{ .Release.Namespace }}
  labels:
    app: "{{ include "planka.fullname" . }}"
    component: pvc
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50M

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ include "planka.fullname" . }}-attachments"
  namespace: {{ .Release.Namespace }}
  labels:
    app: "{{ include "planka.fullname" . }}"
    component: pvc
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500M

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ include "planka.fullname" . }}"
  namespace: {{ .Release.Namespace }}
  labels:
    app: "{{ include "planka.fullname" . }}"
    component: configmap
data:
  NODE_ENV: production
  DEFAULT_LANGUAGE: en-US
  BASE_URL: "http://{{ .Values.planka.hostname }}"
  DEFAULT_ADMIN_NAME: Admin
  TRUST_PROXY: "0"

---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: "{{ include "planka.fullname" . }}"
  namespace: {{ .Release.Namespace }}
  labels:
    app: "{{ include "planka.fullname" . }}"
    component: externalsecret
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: "jdp-backend-secrets"
    kind: ClusterSecretStore
  dataFrom:
  - extract:
      key: planka
  target:
    name: "{{ include "planka.fullname" . }}"
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        SECRET_KEY: "{{ `{{ index . \"secret-key\" }}` }}"
        DATABASE_URL: "postgresql://{{ `{{ index . \"database-username\" }}` }}:{{ `{{ index . \"database-password\" }}` }}@{{ .Values.planka.database.host }}:{{ .Values.planka.database.port }}/{{ .Values.planka.database.name }}"
        DEFAULT_ADMIN_USERNAME: "{{ `{{ index . \"admin-username\" }}` }}"
        DEFAULT_ADMIN_PASSWORD: "{{ `{{ index . \"admin-password\" }}` }}"
        DEFAULT_ADMIN_EMAIL: "{{ `{{ index . \"admin-email\" }}` }}"

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{ include "planka.fullname" . }}"
  namespace: {{ .Release.Namespace }}
  labels: &labels
    app: "{{ include "planka.fullname" . }}"
    component: deployment
spec:
  replicas: 1
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
    spec:
      containers:
      - name: planka
        image: "{{ .Values.planka.image }}"
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 1337
        envFrom:
        - configMapRef:
            name: "{{ include "planka.fullname" . }}"
        - secretRef:
            name: "{{ include "planka.fullname" . }}"
        volumeMounts:
        - name: "favicons"
          mountPath: /app/public/favicons
        - name: "user-avatars"
          mountPath: /app/public/user-avatars
        - name: "background-images"
          mountPath: /app/public/background-images
        - name: "attachments"
          mountPath: /app/private/attachments
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop: [ALL]
        resources:
          limits:
            memory: "2Gi"
      volumes:
      - name: "favicons"
        persistentVolumeClaim:
          claimName: "{{ include "planka.fullname" . }}-favicons"
      - name: "user-avatars"
        persistentVolumeClaim:
          claimName: "{{ include "planka.fullname" . }}-user-avatars"
      - name: "background-images"
        persistentVolumeClaim:
          claimName: "{{ include "planka.fullname" . }}-background-images"
      - name: "attachments"
        persistentVolumeClaim:
          claimName: "{{ include "planka.fullname" . }}-attachments"

---
apiVersion: v1
kind: Service
metadata:
  name: "{{ include "planka.fullname" . }}"
  namespace: {{ .Release.Namespace }}
  labels:
    app: "{{ include "planka.fullname" . }}"
    component: service
spec:
  selector:
    app: "{{ include "planka.fullname" . }}"
    component: deployment
  ports:
  - protocol: TCP
    name: http
    port: 80
    targetPort: 1337

---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: "{{ include "planka.fullname" . }}"
  namespace: {{ .Release.Namespace }}
  labels:
    app: "{{ include "planka.fullname" . }}"
    component: ingress
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`{{ .Values.planka.hostname }}`)
      services:
        - kind: Service
          name: "{{ include "planka.fullname" . }}"
          port: 80
