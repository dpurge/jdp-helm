---
apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ include "pgadmin.fullname" . }}"
  namespace: {{ .Release.Namespace }}
  labels:
    app: "{{ include "pgadmin.fullname" . }}"
    component: configmap
data:
  servers.json: |
    {
      "Servers": {
        "1": {
          "Name": "Homelab",
          "Group": "Servers",
          "Username": "admin",
          "Port": {{ .Values.pgadmin.database.port }},
          "Host": "{{ .Values.pgadmin.database.host }}",
          "SSLMode": "prefer",
          "MaintenanceDB": "postgres"
        }
      }
    }

---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: "{{ include "pgadmin.fullname" . }}"
  namespace: {{ .Release.Namespace }}
  labels:
    app: "{{ include "pgadmin.fullname" . }}"
    component: externalsecret
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: "jdp-backend-secrets"
    kind: ClusterSecretStore
  target:
    name: "{{ include "pgadmin.fullname" . }}"
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: pgadmin

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{ include "pgadmin.fullname" . }}"
  namespace: {{ .Release.Namespace }}
  labels: &labels
    app: "{{ include "pgadmin.fullname" . }}"
    component: deployment
spec:
  replicas: 1
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
    spec:
      containers:
        - name: pgadmin
          image: "{{ .Values.pgadmin.image }}"
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          env:
            - name: PGADMIN_DEFAULT_EMAIL
              valueFrom:
                secretKeyRef:
                  name: "{{ include "pgadmin.fullname" . }}"
                  key: admin-email
            - name: PGADMIN_DEFAULT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "{{ include "pgadmin.fullname" . }}"
                  key: admin-password
          securityContext:
            runAsUser: 0
            allowPrivilegeEscalation: true
          resources:
            limits:
              memory: 256Mi
          volumeMounts:
          - name: "config"
            mountPath: /pgadmin4/servers.json
            subPath: servers.json
            readOnly: true
          - name: "data"
            mountPath: /var/lib/pgadmin
      volumes:
      - name: "config"
        configMap:
          name: "{{ include "pgadmin.fullname" . }}"
      - name: "data"
        emptyDir:
          sizeLimit: 10Mi

---
apiVersion: v1
kind: Service
metadata:
  name: "{{ include "pgadmin.fullname" . }}"
  namespace: {{ .Release.Namespace }}
  labels:
    app: "{{ include "pgadmin.fullname" . }}"
    component: service
spec:
  selector:
    app: "{{ include "pgadmin.fullname" . }}"
    component: deployment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: "{{ include "pgadmin.fullname" . }}"
  namespace: {{ .Release.Namespace }}
  labels:
    app: "{{ include "pgadmin.fullname" . }}"
    component: ingress
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`{{ .Values.pgadmin.hostname }}`)
      services:
        - kind: Service
          name: "{{ include "pgadmin.fullname" . }}"
          port: 80
